---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---
<time class="article-metas__date" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
